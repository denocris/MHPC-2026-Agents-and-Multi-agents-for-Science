# ======================================================================
# Python dependencies for the course:
#   "From Single Agents to Multi-Agent Systems"
#
# Target: Python 3.11+
#
# Install with:
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r setup/requirements.txt
#
# Versions are pinned to guarantee reproducibility across student laptops.
# Ollama itself is NOT a pip package — it must be installed separately
# (see setup/setup_env.sh).
# ======================================================================

# --- Core scientific stack (used everywhere: simulation + analysis) ---
numpy==1.26.4
scipy==1.13.1
matplotlib==3.9.2
pandas==2.2.2

# --- LLM clients / HTTP ---
requests==2.32.3
httpx==0.27.2

# --- Agent frameworks ---
# smolagents: Day 1 single-agent (ReAct, tool use, MCP client)
smolagents==1.10.0
# LiteLLM: unified client used by smolagents/CrewAI to talk to Ollama
litellm==1.51.0
# CrewAI: Day 2 multi-agent
crewai==0.80.0
crewai-tools==0.14.0

# --- RAG stack (Module 1.2) ---
chromadb==0.5.15
sentence-transformers==3.2.1
pypdf==5.0.1
pdfplumber==0.11.4
tiktoken==0.8.0

# --- MCP (Module 1.3) ---
# Official Model Context Protocol Python SDK
mcp==1.0.0

# --- arXiv search (Module 1.3) ---
arxiv==2.1.3

# --- A2A protocol / agent servers (Module 2.3) ---
fastapi==0.115.4
uvicorn==0.32.0
pydantic==2.9.2

# --- Dev / notebook environment ---
jupyter==1.1.1
jupyterlab==4.2.5
ipykernel==6.29.5
ipywidgets==8.1.5
tqdm==4.66.5
rich==13.9.3

# --- Safe expression evaluator for the calculator tool (Module 1.1) ---
simpleeval==1.0.3

# --- Testing ---
pytest==8.3.3
